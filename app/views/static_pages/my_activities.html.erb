<% title "Activity" %>
<% page_md unless admin_signed_in? %>
<%= render "users/nav", selected: :reimbursements unless admin_signed_in? %>
<% @no_app_shell = true if admin_signed_in? %>

<h2 class="heading">Recently on HCB...</h2>

<%= render partial: "static_pages/my_activities" if Flipper.enabled?(:recently_on_hcb_2024_05_23, current_user) && @activities %>
