<% report = @commentable %>

<%= @comment.user.name %> <%= @comment.action_text %> on <i><%= report.name %></i> in <i><%= report.event.name %></i>:

<blockquote><%== MarkdownService.instance.renderer.render @comment.content %></blockquote>

<%= link_to "Click here to view and reply to the comment on HCB.", reimbursement_report_url(report, hash: "comment-#{@comment.hashid}") %>
