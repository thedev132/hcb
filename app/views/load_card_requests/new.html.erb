<% title 'Transfer money' %>
<% page_sm %>
<%= render 'events/nav' %>

<h1>Move money to card balance</h1>

<div class="flex flex-wrap justify-between mt3">
  <div class="stat muted mb1">
    <span class="stat__label">Bank balance</span>
    <span class="stat__value"><%= render_money @event.balance, '' %></span>
  </div>
  <span class="flex-auto h1 pl2 pr2 md-center">→</span>
  <div class="stat mb1">
    <span class="stat__label">Card balance</span>
    <span class="stat__value"><%= render_money @event.emburse_balance, '' %></span>
  </div>
</div>

<p class="black bold mt1 mb0">
  The average time of the last five transfers to Emburse is
  <span class="primary"><%= pluralize LoadCardRequest.processing_time, 'day' %></span>.
</p>
<p class="slate h5 mt0">
  Transfers may take longer than the current average. Sadly
  this is beyond our control, constrained by the US banking /
  ACH transfer system.<br />
  Please plan accordingly.
</p>

<article class="card mt3">
  <%= render 'form', load_card_request: @load_card_request %>
</article>
