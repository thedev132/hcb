<% title 'Unsettled canonical transactions' %>

<h1>Unsettled canonical pending transactions (wip)</h1>

<table>
  <thead class="pr1">
    <tr>
      <th>ID</th>
      <th>DATE</th>
      <th>MEMO</th>
      <th class="right-align">AMOUNT</th>
      <th class="pr2"></th>
    </tr>
  </thead>
  <tbody>
  <% @canonical_pending_transactions.each do |t| %>
    <tr class="<%= "shade-red" if t.date <= 2.weeks.ago %>">
      <td style="width: 1%;"><%= t.id %></td>
      <td><%= t.date %></td>
      <td>PENDING: <%= t.memo %></td>
      <td class="right-align"><%= t.amount %></td>
      <td class="pr2" style="width: 1%;">
        <%= link_to t.event.name, event_path(t.event) %>
      </td>
    </tr>
  <% end %>
  </tbody>
</table>
