A charge of <%= render_money @cpt.amount_cents %> at '<%= @cpt.memo %>' occurred on your card ending in <%= @cpt.stripe_card.last4 %>.

<% if @cpt.local_hcb_code.receipt_required? %>
  <%= link_to "Upload your receipt for this charge (this link can used by anyone for two weeks)", @upload_url %>.

  <% if @receipt_upload_feature %>
    <p>
      Alternatively, <em>reply to this email with an image or PDF receipt attached</em>. When responding with a receipt, include a line beginning with <em>@rename</em> to rename the transaction.
    </p>
  <% end %>
<% end %>
