<% if report.expenses.any? %>
  <% report.expenses.order(created_at: :asc).includes(receipts: { file_attachment: :blob }).each do |expense| %>
    <%= render "reimbursement/expenses/expense", expense:, new: @editing == expense.id %>
  <% end %>
<% end %>
