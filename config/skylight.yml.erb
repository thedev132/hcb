---
# The authentication token for the application.
# authentication: <%= Rails.application.credentials.skylight %>

# (max): Skylight stopped reporting about a year ago without any config
# changes. I've gotten it working again by setting an environmental variable on
# heroku. Keeping this file for posterity to know how skylight is configured
# now
